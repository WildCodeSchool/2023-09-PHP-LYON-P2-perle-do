{# TODO : manage errors display #}

<form method="post">
	<input type="hidden" value="{{ customer.id }}" name="id">
	<label for="title">Tile</label>
	<input type="text" value="{{ customer.title }}" name="title" id="title">
	<button>Submit</button>
</form>
